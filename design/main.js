const authenticationButton = document.querySelector(".submit-btn");

authenticationButton.addEventListener("click", handleAuthenticationFormSubmit);


function handleAuthenticationFormSubmit(e) {
    console.log(e);
}
